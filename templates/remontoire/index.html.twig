{% extends 'base.html.twig' %}

{% block title %}Mes Remontoires{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font-family: Arial, sans-serif;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        h1 {
            background-color: #333;
            color: #fff;
            padding: 15px;
            margin: 0;
        }

        ul {
            list-style: none;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Mes Remontoires</h1>
        <table>
            <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Membre</th>
            </tr>
            </thead>
            <tbody>
            {% for remontoire in remontoires %}
                <tr>
                    <td>{{ remontoire.id }}</td>
                    <td>{{ remontoire.nom }}</td>
                    <td>{{ remontoire.member.nom }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">Pas d'enregistrements trouvés</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ '/Users/adamramsis/PhpstormProjects/watchplace/src/Controller/RemontoireController.php'|file_link(0) }}">src/Controller/RemontoireController.php</a></code></li>
            <li>Your template at <code><a href="{{ '/Users/adamramsis/PhpstormProjects/watchplace/templates/remontoire/index.html.twig'|file_link(0) }}">templates/remontoire/index.html.twig</a></code></li>
        </ul>
    </div>
{% endblock %}
